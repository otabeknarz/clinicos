# ClinicOS — muhit sozlamalari
#
# Bu faylni nusxalab `.env` deb nomlang va qiymatlarni to'ldiring.
# `.env` git'ga tushmaydi (`.gitignore` da).

# Backend manzili.
#
# BO'SH QOLDIRILSA — ilova DEMO (mock) rejimda ishlaydi: barcha
# ma'lumot brauzerda generatsiya qilinadi, serverga bitta ham so'rov
# ketmaydi. Dizaynni ko'rsatish va sinash uchun shu yetarli.
#
# Manzil berilishi bilan har bir funksiya haqiqiy so'rovga o'tadi.
# Endpointlar ro'yxati — docs/API.md
#
# VITE_API_URL=https://api.clinicos.uz
